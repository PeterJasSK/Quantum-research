# Quantum Galton Board — runtime dependencies (not vendored).
#
# Core (config, walk, walk_spec, pipeline, layout, galton ideal arm, walk_check):
# stdlib + the two below only. The offline gate and the root-free ideal arm run
# aer-free (OQ-2.5). The hw arm additionally reuses the CalibrationGuidedHighYieldQRNG
# submission pipeline (pipeline_common), which pulls qiskit-ibm-runtime.
qiskit>=2.0
numpy

# noisy arm only (P2, OQ-2.1): AerSimulator.from_backend device noise model.
# Imported lazily in arms._run_noisy, so its absence never blocks ideal/offline.
qiskit-aer>=0.15

# hw arm only: live Heron r2 access via the reused pipeline_common.
qiskit-ibm-runtime

# P4 figures only: imported solely by figures.py (matplotlib.use("Agg")). The
# sweep/aggregate (sweep.py) + replay export (replay_export.py) paths never
# import it, so the depth sweep still runs headless without matplotlib present.
matplotlib>=3.8
