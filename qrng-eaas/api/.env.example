# Neon pooled connection string (psycopg connects/closes per invocation).
DATABASE_URL=

# Upstash Redis, backs the atomic drbg:counter used for output uniqueness.
REDIS_URL=

# 256-bit quantum master key, hex-encoded (32 bytes = 64 hex chars).
# Sub-keys (e.g. pool-encryption-key) are HKDF-SHA256-derived from this;
# it is never written to Neon and never used directly as a DRBG seed.
MASTER_KEY=

# Bearer token guarding admin-only routes (ingest, etc.) in later epics.
ADMIN_TOKEN=
